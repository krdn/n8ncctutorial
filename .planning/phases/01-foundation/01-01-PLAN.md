---
phase: 01-foundation
plan: 01
type: execute
depends_on: []
files_modified: [package.json, tsconfig.json, src/index.ts, .gitignore, .nvmrc]
---

<objective>
TypeScript 프로젝트 기본 구조 초기화

Purpose: n8n 워크플로우 매니저의 기반이 되는 TypeScript/Node.js 프로젝트 구조를 설정한다.
Output: npm 프로젝트, TypeScript 설정, 기본 진입점 파일
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Tech stack:**
- Node.js (v20 LTS)
- TypeScript 5.x
- ES Modules (type: module)

**Constraints:**
- CLI 도구로 사용될 예정이므로 ES Modules 사용
- n8n MCP 서버와 연동 예정
</context>

<tasks>

<task type="auto">
  <name>Task 1: npm 프로젝트 초기화</name>
  <files>package.json, .nvmrc, .gitignore</files>
  <action>
npm init -y로 package.json 생성 후 수정:
- name: "n8n-workflow-manager"
- version: "0.1.0"
- type: "module" (ES Modules 사용)
- main: "dist/index.js"
- bin: { "n8n-wfm": "dist/index.js" }
- scripts: build, dev, clean
- engines: { "node": ">=20.0.0" }

.nvmrc 파일에 "20" 기록
.gitignore에 node_modules, dist, .env, *.log 추가
  </action>
  <verify>cat package.json | grep '"type": "module"' && cat .nvmrc</verify>
  <done>package.json에 type:module 설정, .nvmrc 존재, .gitignore 설정 완료</done>
</task>

<task type="auto">
  <name>Task 2: TypeScript 설정</name>
  <files>tsconfig.json</files>
  <action>
tsconfig.json 생성:
- target: ES2022
- module: NodeNext
- moduleResolution: NodeNext
- outDir: ./dist
- rootDir: ./src
- strict: true
- esModuleInterop: true
- skipLibCheck: true
- declaration: true
- declarationMap: true

typescript, @types/node 개발 의존성으로 설치
  </action>
  <verify>npx tsc --version && cat tsconfig.json</verify>
  <done>TypeScript 설치됨, tsconfig.json 생성됨, strict 모드 활성화</done>
</task>

<task type="auto">
  <name>Task 3: 진입점 파일 생성</name>
  <files>src/index.ts</files>
  <action>
src/index.ts 생성:
- #!/usr/bin/env node shebang 추가
- 기본 console.log로 "n8n Workflow Manager v0.1.0" 출력
- main 함수 구조 준비

빌드 테스트: npm run build
  </action>
  <verify>npm run build && node dist/index.js</verify>
  <done>빌드 성공, dist/index.js 실행 시 버전 출력</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` 성공
- [ ] `node dist/index.js` 실행 시 버전 출력
- [ ] TypeScript strict 모드 에러 없음
- [ ] ES Modules 정상 동작
</verification>

<success_criteria>
- 모든 태스크 완료
- 빌드 성공
- 기본 실행 테스트 통과
</success_criteria>

<output>
완료 후 `.planning/phases/01-foundation/01-01-SUMMARY.md` 생성
</output>
